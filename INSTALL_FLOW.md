# INSTALL_FLOW

HYPOTHESIS: Factory.ai’s shared installer surface points to the public npm install path.

## Public claims supported by the frozen snapshots
- `snapshots/app-factory-ai-cli.md`: the app landing page exposes the install command and links to Missions and the news post.
- `snapshots/docs-factory-ai-cli-features-missions.md`: Missions is the guided CLI workflow and the installer surface points at the public npm install path.
- `snapshots/factory-ai-news-missions.md`: the news post frames Missions as a launch announcement and repeats the same public installer language.

HYPOTHESIS: Taken together, these frozen sources show a consistent public installation flow for Factory.ai without relying on live docs.