# MISSIONS_FINDINGS

- `snapshots/app-factory-ai-cli.md`: the app landing page exposes the install command and links out to Missions and the news post.
- `snapshots/docs-factory-ai-cli-features-missions.md`: Missions is presented as the guided CLI workflow, and the installer surface points at the public npm install path.
- `snapshots/factory-ai-news-missions.md`: the news post frames Missions as a launch announcement and repeats the same public installer language.

These citations are frozen and should be read only as checked-in evidence from the listed snapshot paths.